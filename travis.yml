language: node_js
install:
  - nvm install node
  - node -v
  - npm i -g yarn
  - yarn
  - npm install --save-dev jest
  - npm i jest-fetch-mock
  - npm install express
  - npm install body-parser
script:
  - yarn test
